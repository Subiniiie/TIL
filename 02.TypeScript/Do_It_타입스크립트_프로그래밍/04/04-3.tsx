//  화살표 함수와 표현식 문

const arrow1 = (a: number, b: number): number => {return a + b};
const arrow2 = (a: number, b: number): number => a + b;

// 실행된 결과를 알려면 반드시 return 키워드 사용
const isGreater = (a: number, b: number): boolean => {
    return a > b;
};
